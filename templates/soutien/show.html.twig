{% extends 'adminbase.html.twig' %}

{% block title %}Soutien{% endblock %}

{% block body %}
    <h1>Soutien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ soutien.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ soutien.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ soutien.prenom }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ soutien.numero }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ soutien.email }}</td>
            </tr>
            <tr>
                <th>Choix</th>
                <td>{{ soutien.choix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('soutien_inscris') }}" class="btn btn-info">back to list</a>

    {# <a href="{{ path('soutien_edit', {'id': soutien.id}) }}">edit</a> #}

    {{ include('soutien/_delete_form.html.twig') }}
{% endblock %}
