{% extends 'base.html.twig' %}

{% block title %}FormationScp{% endblock %}

{% block body %}
    <h1>FormationScp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation_scp.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ formation_scp.titre }}</td>
            </tr>
            <tr>
                <th>Info</th>
                <td>{{ formation_scp.info }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('formation_scp_index') }}">back to list</a>

    <a href="{{ path('formation_scp_edit', {'id': formation_scp.id}) }}">edit</a>

    {{ include('formation_scp/_delete_form.html.twig') }}
{% endblock %}
